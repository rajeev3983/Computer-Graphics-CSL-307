1. Type make, it will generate rasterize
2. Run ./rasterize input 
3. Source file is named as rasterize.c
4. Range of x axis is -width/2 to width/2 where width is the width of the window in pixels
5. Range of y axis is -height/2 to height/2 where height is the height of the window in pixels
6. Origin of the screen (0,0) is at the centre of the screen
7. R G B A values for any pixel should be between 0 and 255
